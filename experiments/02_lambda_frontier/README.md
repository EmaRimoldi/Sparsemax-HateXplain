# 02 - Lambda frontier

This validation-only pilot traces the utility--plausibility response curve for the
four target/loss combinations with human rationales. It uses one seed to restrict
compute. The selected lambda set is then frozen and rerun with five seeds only for
the Pareto-relevant conditions; no test metric may influence selection.
